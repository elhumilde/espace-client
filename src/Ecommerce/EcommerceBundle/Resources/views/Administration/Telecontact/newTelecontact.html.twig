{% extends '::layout/layoutAdmin.html.twig' %}

{% block body -%}
    <h1>Ajouter  Telecontact</h1>

    <form action="{{ path('adminTelecontact_create') }}" method="POST" {{ form_enctype(form) }}>
        {{ form(form) }}
    </form>

    <ul class="record_actions">
        <li>
            <a href="{{ path('adminTelecontact') }}">
                Back to the list
            </a>
        </li>
    </ul>
{% endblock %}